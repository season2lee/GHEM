server:
  port: 80
#kakao:
#  oauth:
#    ApiKey: ecec52bbf725b72025bc351c6986a44b
#    redirectURI: http://localhost:8080/auth/kakao/callback

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jtest?useUniCode=yes&characterEncoding=UTF-8&serverTimezone=Asia/Seoul&allowMultiQueries=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password : root

  jpa:
    hibernate:
      ddl-auto: create #???? ???? ??? ?? ??
    properties:
      hibernate: # hibernate? ??? ??? property? ??? ? ??
        #        show_sql: true System.out? ??? sql? ?? ? ??? logger? ??? ?? ? ??? ?? ???.
        format_sql: true

logging:
  level:
    org.hibernate.sql : debug # JPA? hibernate? ???? SQL? ? ???.
    org.hibernate.type : trace